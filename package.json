{
  "name": "organ-runtime-v0",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run src/index.ts",
    "turn": "bun run src/index.ts",
    "test:mock": "bun run scripts/mock-test.ts",
    "reset": "rm -rf state && mkdir -p state"
  },
  "engines": {
    "bun": ">=1.1.0"
  }
}
