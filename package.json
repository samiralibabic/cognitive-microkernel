{
  "name": "cognitive-microkernel",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run src/index.ts",
    "turn": "bun run src/index.ts",
    "reset": "rm -rf state && mkdir -p state"
  },
  "engines": {
    "bun": ">=1.1.0"
  },
  "devDependencies": {
    "typescript": "^6.0.3"
  }
}
