{
  "name": "cognitive-microkernel",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run src/index.ts",
    "chat": "bun run src/index.ts --chat",
    "reset": "rm -rf state && mkdir -p state",
    "test:harness": "bun run scripts/regression-tool-loop.ts"
  },
  "engines": {
    "bun": ">=1.1.0"
  },
  "devDependencies": {
    "typescript": "^6.0.3"
  }
}
